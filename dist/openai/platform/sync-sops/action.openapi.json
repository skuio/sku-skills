{
  "openapi": "3.1.0",
  "info": {
    "title": "SKU.io — Sync your SOPs with SKU.io changes",
    "description": "Keep your internal Standard Operating Procedures (SOPs) in step with SKU.io. SKU.io ships often — menus move, workflows change, features are added — which quietly breaks the step-by-step procedures your team relies on. Give this skill your SOP content plus the date you last reviewed it: it reads SKU.io's public changelog and documentation, finds the changes that affect your procedures, and rewrites the exact stale steps — or drafts a brand-new SOP from the current docs, in your own format. Use it whenever internal docs/SOPs may be out of date, or on a schedule to catch drift.",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "https://{tenant}.sku.io",
      "variables": {
        "tenant": {
          "default": "app",
          "description": "Your SKU.io account subdomain."
        }
      }
    }
  ],
  "security": [
    {
      "bearerAuth": []
    }
  ],
  "paths": {
    "/api/auth/profile": {
      "get": {
        "operationId": "verify_connection",
        "summary": "Confirm the SKU.io connection and identify the tenant/account so SOPs can be grounded in the customer's real setup (their warehouses, custom fields, pricing tiers). OPTIONAL — reading the public documentation and changelog (docs.sku.io) needs no token; only account-specific grounding does.",
        "responses": {
          "200": {
            "description": "Successful response"
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "description": "SKU.io Personal Access Token."
      }
    }
  }
}
