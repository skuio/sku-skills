<!-- Thanks for contributing to sku-skills! Keep PRs focused: one skill or one fix. -->

## What does this PR do?

<!-- e.g. "Adds the `returns/create-rma` skill" or "Fixes the endpoint in `find-product`" -->

## Skill(s) touched

- Domain / name:
- New skill or fix:

## SKU.io endpoints used

<!-- List the paths + methods this skill calls, e.g. `POST /api/sales-orders`. -->

- [ ] These are **real** endpoints from https://developer.sku.io (not guessed)
- [ ] I tested them against a live tenant with a scoped PAT — <!-- yes / no; if no, explain -->

## Checklist

- [ ] `npm run check` passes (validate + build)
- [ ] `auth.scopes` are least-privilege
- [ ] `INSTRUCTIONS.md` has decision logic, a concrete request, response handling, and guardrails
- [ ] I did **not** commit `dist/`, `node_modules/`, or any secrets/tenant data
- [ ] Canonical source only (`skills/` / `shared/`)
